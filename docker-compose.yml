services:
  app:
    build: .
    container_name: yelpcamp-app
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=8000
    ports:
      - "8000:8000"
    restart: unless-stopped
